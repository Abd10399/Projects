This application simulates a memory module in which we use a Linked List to store different strings.
This allows us to view a low level abstraction of memory management done by the OS and how different we keep account 
of different strings (or segments) in main memory.